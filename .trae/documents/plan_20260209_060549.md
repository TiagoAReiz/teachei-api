I will implement the authentication state management and update the app icon as requested.

### Plan

1.  **Update Landing Page (`teachei-web/components/landing/landing-page.tsx`)**
    *   Integrate the `useAuth` hook to check the user's authentication status.
    *   Modify the header buttons logic:
        *   **If authenticated**: Display a "Ir para o Feed" button that redirects to `/feed`.
        *   **If not authenticated**: Retain the existing "Entrar" and "Criar Conta" buttons.

2.  **Protect Login & Register Pages**
    *   **Login Page (`teachei-web/app/(auth)/login/page.tsx`)**: Add a redirection check. If the user is already authenticated (and loading is complete), automatically redirect them to `/feed`.
    *   **Register Page (`teachei-web/app/(auth)/register/page.tsx`)**: Apply the same redirection logic as the login page.

3.  **Update App Icon**
    *   Replace the current app icon (`teachei-web/app/icon.png`) with the new image provided (`teachei-web/public/logofundo.png`). This ensures the new logo is used as the favicon and app icon across the site.

### Verification
*   I will verify the code changes by reviewing the logic for authentication checks and redirections.
*   I will confirm the file operation for the icon replacement.
