# Ajustes Visuais e Retorno da Busca ao Header

Atualizei o plano para incluir a reversão da barra de busca, além dos ajustes visuais solicitados.

## 1. Retornar Busca ao Cabeçalho (`Header`)
Vou reverter a mudança anterior e trazer a barra de busca de volta para o cabeçalho fixo, garantindo que ela esteja acessível em todas as páginas.
- **Header**: Reintegrar o `SearchInput` (versão Desktop centralizada e Mobile flutuante/expandida).
- **Feed**: Remover a barra de busca duplicada do topo da lista de intenções.
- **Correção de Z-Index**: Manterei os ajustes de `z-index` feitos anteriormente (menus com 1000+) para garantir que a busca não fique por cima dos dropdowns de notificação e perfil.

## 2. Melhoria no Fundo do Login/Cadastro (`AuthLayout`)
Ajustar o fundo da área da imagem para um gradiente suave.
- **Ação**: Substituir o fundo sólido por um gradiente `from-primary/90` (topo) para `to-transparent` (base) ou similar, permitindo ver a imagem do carro e destacando a logo sem cobri-la com uma cor sólida chapada.

## 3. Ajuste de Margem no Modal de Filtros (`FilterPanel`)
Descolar o filtro do fundo da tela no mobile.
- **Ação**: Definir altura máxima no mobile e adicionar margem inferior (`mb-20` ou similar) e arredondamento, para que ele pareça um card flutuante e não cubra a navegação inferior.

## 4. Remoção do Botão "Anunciar" no Header Mobile
Remover o botão `+` do topo em telas pequenas, já que ele existe na barra inferior.
- **Ação**: Ocultar botão de criar anúncio no header mobile.